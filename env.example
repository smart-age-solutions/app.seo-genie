# NextAuth.js
# Note: Database connection is handled by the backend API
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-key-here-change-in-production"

# Google OAuth (configured via Admin Dashboard - these are optional fallbacks)
# GOOGLE_CLIENT_ID="your-google-client-id"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Backend API URL
# ---
# NEXT_PUBLIC_API_URL: Public URL for client-side (browser) requests
# BACKEND_API_URL: Internal URL for server-side API routes (Docker network or same server)
# ---
# Development (Docker):
#   NEXT_PUBLIC_API_URL=http://localhost:3001  (for any direct browser calls)
#   BACKEND_API_URL=http://backend:3001        (internal Docker network)
#
# Production (Vercel + Fly.io):
#   NEXT_PUBLIC_API_URL=https://your-backend.fly.dev
#   BACKEND_API_URL is not needed (uses NEXT_PUBLIC_API_URL)
# ---
NEXT_PUBLIC_API_URL="http://localhost:3001"
# BACKEND_API_URL="http://backend:3001"  # Only for Docker development
